version: '2'
services:
  go-web-app:
    restart: always
    build:
      dockerfile: Dockerfile
      context: .
    volumes:
      - ./static:/usr/static:ro
    environment:
      - VIRTUAL_HOST=hickscaleb.com
      - LETSENCRYPT_HOST=hickscaleb.com
